# Model artifacts

`random_forest.joblib` is generated by `python train_model.py` and loaded by FastAPI by default. Set `AI_MODEL_PATH` to deploy another artifact. The file must be generated with `train_random_forest.py` so that it contains the model and required metadata.
